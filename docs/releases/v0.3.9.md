# v0.3.9

Released 2026-06-08. Source of truth: git tag `v0.3.9` (annotated).
This file mirrors the GitHub release notes at
<https://github.com/liyown/skills-registry/releases/tag/v0.3.9>.

## Installation

```sh
npx skills add liyown/skills-registry#v0.3.9 --skill java-code-reviewer
```

Or install the full set:

```sh
npx skills add liyown/skills-registry#v0.3.9
```

## Highlights

- Router strengthening extended to the two meta-skills.
  `goal-driven-development` description now ends with
  "minimizing the risk of a reverted merge, a missed
  dependency, or a code-review block at PR time".
  `project-knowledge-capture` description now ends with
  "so the next developer (or the next session) can answer
  'why was this done' and 'what to be careful about' in
  minutes, not days".
- All 7 skills now end their frontmatter `description` with
  a severity-tier router clause. Rolled out across 3 commits:
  v0.3.8 for the 5 reviewers, v0.3.9 for the 2 meta-skills.

## Commits since v0.3.8

```
7e9e6bd docs(meta-skill): strengthen frontmatter description as a router
```

1 substantive commit.

## Verification at tag time

- `./scripts/validate.sh` exit=0 (smoke / examples / validation).
- `release-readiness` CI workflow green (idempotency check
  passes on the release commit too).
- All 7 skill frontmatter descriptions confirmed to contain
  a severity-tier router clause (`grep -l 'incident-causing\|reverted merge\|minutes, not days' skills/*/SKILL.md`).

## Full Changelog

See [docs/CHANGELOG.md](../CHANGELOG.md) for the cumulative release
history. The diff for v0.3.8 → v0.3.9 is `git log v0.3.8..v0.3.9`.

See [INDEX.md](INDEX.md) for a cross-reference of every release.
