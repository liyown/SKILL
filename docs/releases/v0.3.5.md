# v0.3.5

Released 2026-06-08. Source of truth: git tag `v0.3.5` (annotated).
This file mirrors the GitHub release notes at
<https://github.com/liyown/skills-registry/releases/tag/v0.3.5>.

## Installation

```sh
npx skills add liyown/skills-registry#v0.3.5 --skill java-code-reviewer
```

Or install the full set:

```sh
npx skills add liyown/skills-registry#v0.3.5
```

## Highlights

- `docs/releases/INDEX.md` — cross-reference for every release.
  Lists tag, date, commit count since the previous tag, the
  GitHub release URL, and the in-repo mirror file path. Also
  documents the full release flow and where to find each kind of
  release artefact. v0.3.1+ have mirrors; v0.3.0 and earlier are
  linked to GitHub only (pre-script era).

## Commits since v0.3.4

```
963fad7 docs: add cross-reference index for all releases
```

1 substantive commit.

## Verification at tag time

- `./scripts/validate.sh` exit=0 (smoke / examples / validation).
- `release-readiness` CI workflow green on the commit.

## Full Changelog

See [docs/CHANGELOG.md](../CHANGELOG.md) for the cumulative release
history. The diff for v0.3.4 → v0.3.5 is `git log v0.3.4..v0.3.5`.

See [INDEX.md](INDEX.md) for a cross-reference of every release.
