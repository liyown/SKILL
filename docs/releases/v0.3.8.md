# v0.3.8

Released 2026-06-08. Source of truth: git tag `v0.3.8` (annotated).
This file mirrors the GitHub release notes at
<https://github.com/liyown/skills-registry/releases/tag/v0.3.8>.

## Installation

```sh
npx skills add liyown/skills-registry#v0.3.8 --skill java-code-reviewer
```

Or install the full set:

```sh
npx skills add liyown/skills-registry#v0.3.8
```

## Highlights

- All 5 reviewer `SKILL.md` frontmatter `description` fields
  extended with a router clause that names the severity tier:
  "focusing on capital-loss, data-corruption, authorization-
  bypass, and incident-causing defects" (and the frontend
  equivalent "user-data-leak, XSS, authz-bypass" for
  react-code-reviewer). The trigger list now also includes
  "any review where a production incident is the worst case".

## Commits since v0.3.7

```
d443a3f docs(reviewer): extend incident clause to python/node/react
5f6946e docs(reviewer): strengthen frontmatter description as a router
```

2 substantive commits.

## Verification at tag time

- `./scripts/release.sh --bump-mode minor --dry-run` printed
  `v0.4.0` and the `would run` block (release was then run
  with default patch mode for v0.3.8).
- `./scripts/validate.sh` exit=0 (smoke / examples / validation).
- `release-readiness` CI workflow green (idempotency check
  added in v0.3.7 passes on the release commit too).

## Full Changelog

See [docs/CHANGELOG.md](../CHANGELOG.md) for the cumulative release
history. The diff for v0.3.7 → v0.3.8 is `git log v0.3.7..v0.3.8`.

See [INDEX.md](INDEX.md) for a cross-reference of every release.
