# v0.3.7

Released 2026-06-08. Source of truth: git tag `v0.3.7` (annotated).
This file mirrors the GitHub release notes at
<https://github.com/liyown/skills-registry/releases/tag/v0.3.7>.

## Installation

```sh
npx skills add liyown/skills-registry#v0.3.7 --skill java-code-reviewer
```

Or install the full set:

```sh
npx skills add liyown/skills-registry#v0.3.7
```

## Highlights

- `scripts/release.sh` adds `--bump-mode patch|minor|major`. The
  default is `patch` (preserves prior behaviour). `minor`
  resets the patch component to 0 and increments the minor;
  `major` resets both minor and patch to 0 and increments the
  major. `--bump X.Y.Z` still works and overrides the mode for
  non-monotonic cases. The complete release flag surface is
  now 7 flags.

## Commits since v0.3.6

```
3884bba chore(release): add --bump-mode patch|minor|major to release.sh
```

1 substantive commit.

## Verification at tag time

- `./scripts/release.sh --bump-mode minor --dry-run` prints
  `v0.4.0` and the `would run` block, without creating a tag.
- `./scripts/release.sh --bump-mode major --dry-run` prints
  `v1.0.0` and the `would run` block.
- `--bump-mode garbage --dry-run` is rejected with the
  `FAIL: --bump-mode must be patch, minor, or major` error.
- `./scripts/validate.sh` exit=0 (smoke / examples / validation).
- `release-readiness` CI workflow green.

## Full Changelog

See [docs/CHANGELOG.md](../CHANGELOG.md) for the cumulative release
history. The diff for v0.3.6 → v0.3.7 is `git log v0.3.6..v0.3.7`.

See [INDEX.md](INDEX.md) for a cross-reference of every release.
