# v0.3.1

Released 2026-06-08. Source of truth: git tag `v0.3.1` (annotated).
This file mirrors the GitHub release notes at
<https://github.com/liyown/skills-registry/releases/tag/v0.3.1>.

## Installation

```sh
npx skills add liyown/skills-registry#v0.3.1 --skill java-code-reviewer
```

Or install the full set:

```sh
npx skills add liyown/skills-registry#v0.3.1
```

## Highlights

- `scripts/release.sh` — tag-driven release flow. Reads `git describe`,
  bumps the patch component, runs `./scripts/validate.sh`, creates an
  annotated tag, and pushes both the branch and the tag to origin.
- `docs:` Reviewer Coverage Matrix footer links to the per-prompt
  `> See also:` lines so consumers can navigate the matrix index and
  the in-prompt cross-references from either side.
- `docs:` README "Latest Release" section now points at the current
  tag and includes a `git log` hint for the full diff.

## Commits since v0.3.0

```
c3bafb2 docs: bump README Latest Release to v0.3.1
fb8813a chore: add scripts/release.sh for tag-driven release flow
88888a6 docs: link Coverage Matrix to per-prompt See also lines
e98a2e5 docs: add Latest Release section pointing at v0.3.0
```

4 commits; the first is the bump that prompted the tag, so
substantively 3 commits of work between v0.3.0 and v0.3.1.

## Install Behaviour Verified

- `npx skills --help` (v0.86.0) lists `add <package> [--skill <name>]`
  with `npx skills add owner/repo` and `#vX.Y.Z` pinning syntax.
- `./scripts/validate.sh` exits 0 at tag time:
  - `smoke check passed`
  - `examples check passed`
  - `validation passed`

## Full Changelog

See [docs/CHANGELOG.md](../CHANGELOG.md) for the cumulative release
history. The diff for v0.3.0 → v0.3.1 is `git log v0.3.0..v0.3.1`.
